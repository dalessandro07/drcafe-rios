const Carrito = () => {
    return (
        <div className="h-screen w-screen flex justify-center items-center">
            <h1 className="text-3xl text-red-500">Carrito</h1>
        </div>
    );
};

export default Carrito;
